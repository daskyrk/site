// import {
//   autoLoading
// } from '../api/axios'

// export const plugins = [autoLoading('article')]

import service from '../api'

export default {
  state() {
    return {
      fetch: {},
    }
  }
}